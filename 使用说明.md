# AI 用量面板

双击 `UsagePanel.exe` 即可运行，无需安装 Python。

关闭窗口或点击“隐藏”后，程序保留在 Windows 系统托盘。点击绿色柱状图标可显示面板，右键图标可以刷新或退出。若图标被 Windows 收起，在任务栏的向上箭头里查找。

## 面板内容

- **Codex**：读取本机 Codex CLI 登录账户的官方额度。进度条表示剩余比例。5 小时或周窗口仅在接口实际返回时显示，不把缺失窗口当成 0 或 100%。
- **DeepSeek API / via Claude Code**：读取 Claude Code 中已配置的 DeepSeek 密钥，调用官方余额接口。此处为 API 账户余额，不是 Claude 订阅额度。
- **重置卡**：显示 Codex 官方返回的可用张数和最近到期日。「明细」列出接口提供的到期时间；列表可能不完整，不以列表长度推算张数。只展示，不消耗卡片。
- **Codex 官方日用量**：通过官方 `account/usage/read` 读取，直接保留日期和总量，不按本地时区平移。「官方已报」单独显示最近已报值并标注可能延迟，不会替换「本机今日」实时计数。两种口径不相加；官方数据不是逐 token 实时计数。
- **本机明细**：每 10 秒按本地日期读取 Codex 和 Claude Code 中 DeepSeek 日志。输入、输出、缓存命中率仅代表本机保留的记录，不包含其他设备或其他 API 客户端。token 统一用 m（百万），保留三位小数；不足 0.001m 的非零值显示 <0.001m。缓存已包含在输入中，推理 token 已包含在输出中，不重复相加。
- **用量与费用**：查看本机 Codex 的月度折算金额、周期结束预测、主 / 子 Agent、各模型及推理档位、每日趋势和近 60 分钟速度；按下文配置续费锚点。
- **缓存命中率**：面板与明细同步显示，缓存命中 token ÷ 全部输入 token × 100%，保留一位小数。分母含缓存读取、创建及其他输入，不含输出。当天无输入或记录不完整时显示 —。
- **7 天趋势**：点击 SUMMARY 旁「7 天趋势」。Codex 历史采用官方日统计；过去日期未提供显示 —。若今天只有本机暂估，以橙色、≈ 标注，不加入官方合计。官方值出现后直接替换暂估。旧数据标「旧」。DeepSeek 仍按本机本地日期统计，* 表示不完整。两张图分别缩放，单位 m。
- **低额度提醒**：默认开启。Codex 剩余 ≤10%、DeepSeek CNY 余额 ≤10 或 USD 余额 ≤1 时弹出 Windows 托盘通知。在「设置 / 说明」关闭。每 5 分钟随账户刷新检查；同一低状态仅通知一次，恢复后再降低或 Codex 新一轮额度周期才再通知。过期、失败和旧数据不提醒。状态保存于 `data/alert-state.json`，正常重启不重复提醒；Windows 通知设置或勿扰可能隐藏弹窗。不自动充值、不使用重置卡。
- **Summary**：显示系统内存与 Codex、Claude 原生进程数。进程数不等于正在工作的 AI 数量。
- **IQ / 模型评分**：从 Codex Radar 读取 Astra、Sol、Terra、Luna 各推理档位的社区评测分。可切换综合智能、软件工程和视觉空间。综合分与网站一样，按两个维度的有效题量加权；任一维度缺失时不编造综合分。该指标不是人的智商。

## 月度费用与 Agent 分析

打开「用量与费用」，在下方「续费锚点」输入自己的续费日期，格式为 `YYYY-MM-DD`，点击「保存」。程序以这个日期的日号作为每月周期开始日，按本地时区从当天零点统计至下月同日零点，结束日不计入。月末不足该日号时用当月最后一天，后续月份恢复原日号。未设置时不猜测订阅日期；5 小时 / 每周额度重置与这里的月度周期分别显示。

上方概览显示本周期已记录折算、周期结束预计、主 / 子 Agent Token 和最近 60 分钟消耗速度。每日柱状图显示本周期已定价的每日折算金额。下方有三张表：

- **任务 / Agent**：点击任务展开明细，查看主 Agent、子 Agent 的输入、缓存输入、输出、模型及推理档位。同一 Agent 换过模型或档位会分行显示。任务合计已含子 Agent，不能再次相加；未知归属保留为未知。
- **模型排行**：按模型和推理档位分别汇总 Token、在当前筛选结果中的占比及折算费用。`ultra` 等档位来自实际记录，不视为另一种有独立单价的模型。
- **每日明细**：按本地日期显示 Token、已知折算费用及未定价 Token。

搜索框支持任务 / Agent 名称、标识等元数据；可选模型、角色及本周期 / 今日 / 最近 7 天范围，点击表头可排序。时间筛选只覆盖当前载入的月度周期，筛选只改变下方表格，上方概览与趋势仍展示整个周期。

「月度折算预算 $」为选填正数，可随续费锚点一起保存；清空后取消预算。它只对照已记录折算和完整预测，不能当成订阅余额，也不会自动限制调用或充值。近 60 分钟消耗速度使用最近一小时记录，展示折算美元 / 小时与 Token / 分钟，包含该小时内没有调用的时间。

选择标签页并完成筛选后，点击「导出当前表格」保存 CSV 或 JSON。导出包含当前筛选下该表格的明细、原始 Token 数值、已知折算金额和未定价 Token；任务表包含任务名称及 Agent 标识，不含聊天正文。CSV 使用 UTF-8，便于 Excel 打开。导出文件保存在所选位置，分享前可核对其中的任务名称。

### 如何理解金额和预计费用

金额是 **Standard API 基础 Token 折算（USD），不是 ChatGPT / Codex 订阅实付账单**。费率核验日期为 **2026-09-26**，历史记录也按该费率计算，未声称还原当时的真实账单。费率来源：[OpenAI API 定价](https://developers.openai.com/api/docs/pricing)、[GPT-6 Astra](https://developers.openai.com/api/docs/models/gpt-6-astra)、[GPT-5.6 Sol](https://developers.openai.com/api/docs/models/gpt-5.6-sol)、[Terra](https://developers.openai.com/api/docs/models/gpt-5.6-terra)、[Luna](https://developers.openai.com/api/docs/models/gpt-5.6-luna)、[GPT-5.5](https://developers.openai.com/api/docs/models/gpt-5.5)。5.6 Sol 的当前价格属于官方促销价格，后续可能调整。

- 缓存输入是全部输入的一部分，推理 Token 是输出的一部分，均不额外相加。推理档位不另乘费用系数。
- 长上下文使用可辨认的单次请求输入量判断，不能用整项任务的累计 Token 判断。无法确认模型、缓存计数、单次输入或计价条件时，该部分 Token 仍显示，但费用为未定价。
- `金额 + ?` 表示金额仅包括已知部分，问号部分没有被当成免费；全部无法定价时显示「未定价」。
- 当前不计无法确认的 Fast 等服务档位差价、缓存写入溢价以及工具、图像等其他费用。ChatGPT credits 与 API 计费不同，官方额度百分比也不等于美元费用比例，见 [Codex 定价](https://learn.chatgpt.com/docs/pricing)与 [Speed](https://learn.chatgpt.com/docs/agent-configuration/speed)。
- **周期结束预计 = 本周期已记录折算 ÷ 已过时间 × 完整周期时长**。使用实际日历月长度，按截至现在的平均消耗速度外推，不使用 5 小时 / 每周额度比例。
- 周期未满 24 小时、日志读取不完整或没有可定价记录时，不显示数值预测。存在未定价 Token 时，只外推已知部分，并显示「仅已知部分」及 `+ ?`；不会把这个数当成完整总费用。使用节奏变化会改变预测。

月度分析仅覆盖本机 `sessions`、`archived_sessions` 留存日志，不保证覆盖其他电脑、云任务或已删除日志。单次响应与旧版累计事件去重，子 Agent 继承的历史不会再次计入；模型缺失保持未知，不使用当前设置补猜历史模型。任务标题只来自本机会话索引元数据。

## 刷新

账户额度、官方日统计、重置卡、余额和评分启动时读取一次，此后每 5 分钟更新；“刷新”按钮和 Ctrl+R 可手动更新。「本机今日」token 每 10 秒增量读取，跨本地午夜重新统计；底部分别显示额度读取和本机读取的时间，本机计数不会因官方日统计延迟而停留在旧值。重置倒计时每 30 秒在本地更新。隐藏到托盘后仍会刷新。刷新未完成时不会重复启动新的请求。

官方日统计独立缓存，读取失败会保留旧值及原更新时间并标注，不影响其他数据。成功返回空列表时显示未提供，不用旧数据或本机历史补成官方值。其他数据读取失败也保留并标注旧值。窗口过了重置时间，会显示等待刷新，而不是自行假定额度已恢复。评分表的源更新时间和本机查询时间是两种不同时间；综合智能同时采用软件工程和视觉空间两组数据，两个维度的更新可能不同步。

## 数据与配置

程序不发起模型推理、不消费重置卡、不切换 Claude Code 服务，不改变 Codex 或 Claude 的配置。查询额度、重置卡、余额、公开评分，并在本机统计调用日志中的用量字段。不会上传对话或本机 token 统计。

DeepSeek 密钥只在内存中读取，并且只发给 `api.deepseek.com` 官方余额接口；不会发给 Codex Radar，也不会保存进本工具的数据文件。

程序旁的 `data` 文件夹保存脱敏后的额度缓存、官方日统计、余额和评分，以及置顶、评分视图、续费锚点和月度折算预算设置。请将整个目录放在可写的位置。退出并删除本目录即可卸载。默认不设置开机启动。

Codex CLI 需已安装并登录；如使用非标准安装位置，可通过 `USAGE_PANEL_CODEX_EXE` 环境变量指定其可执行文件。本地 token 读取 `CODEX_HOME` 下的 `sessions`、`archived_sessions` 和 `CLAUDE_CONFIG_DIR` 下的 `projects`，默认分别为用户目录下 `.codex`、`.claude`。DeepSeek 优先使用 `DEEPSEEK_API_KEY` 环境变量，其次读取 Claude Code 配置中指向官方 DeepSeek 的密钥。

## 数据来源

- [Codex 官方 App Server](https://learn.chatgpt.com/docs/app-server)
- [OpenAI API 定价](https://developers.openai.com/api/docs/pricing)：Standard 基础 Token 折算参考，核验于 2026-09-26。
- [DeepSeek 官方余额接口](https://api-docs.deepseek.com/zh-cn/api/get-user-balance/)
- [Codex Radar](https://codexradar.com/)：公开的软件工程、视觉空间数据接口。网站接口属于其自身实现；若网站改版，适配器可能需要更新。

源码在 `source` 中。运行依赖：Python 3.12、pystray 0.19.5、Pillow 12.3.0、psutil 7.2.2；可执行文件已打包这些运行依赖。

## 交互参考

参考项目：[ccusage](https://github.com/ccusage/ccusage)、[CodexBar](https://github.com/steipete/CodexBar)、[Claude Code Usage Monitor](https://github.com/Maciek-roboblog/Claude-Code-Usage-Monitor)、[codex-usage](https://github.com/zJay26/codex-usage)。仅借鉴月度报表、速度与明细的交互思路，本项目自行实现，未复制其代码。
